# GHCR Helper Functions

This directory contains standalone shell scripts used as helpers for GHCR-related tasks.

## Scripts
- `delete_image.sh`: Logic for deleting images.
- `get_version.sh`: Logic for retrieving image versions.
- `gh_login.sh`: Handles GitHub CLI login for container registry access.
