# GitHub Container Registry (GHCR) Actions

This directory contains actions and utilities specifically for interacting with the GitHub Container Registry.

## Subdirectories
- `delete/`: Action to delete old images from GHCR.
- `func/`: Utility functions and helper scripts for GHCR operations.
