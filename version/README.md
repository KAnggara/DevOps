# Versioning Action

GitHub Action for managing project versioning.

## Usage

```yaml
- uses: KAnggara/DevOps/version@main
  with:
    path: "./"
```

## Inputs
- `path`: Path of the project (default: `./`).
